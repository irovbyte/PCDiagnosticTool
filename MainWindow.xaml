<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="PCDiagnosticTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:PCDiagnosticTool"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PCDiagnosticTool"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="PCDiagnosticTool">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            PaneDisplayMode="Left"
            SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="Дашборд"
                    Icon="Home"
                    Tag="Dashboard" />
                <NavigationViewItem
                    Content="Журнал и Восстановление"
                    Icon="Repair"
                    Tag="Issues" />
                <NavigationViewItem
                    Content="Логи"
                    Icon="Document"
                    Tag="Logs" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Padding="0,0,0,0" />
        </NavigationView>
    </Grid>
</Window>
